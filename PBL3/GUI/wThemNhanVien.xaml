<Window x:Class="PBL3.GUI.wThemNhanVien"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thêm Nhân Viên Mới" Height="550" Width="400" WindowStartupLocation="CenterScreen" Background="White">
    <Grid Margin="25">
        <StackPanel>
            <TextBlock Text="TẠO TÀI KHOẢN NHÂN VIÊN" FontSize="20" FontWeight="Bold" Foreground="#1A1A1A" HorizontalAlignment="Center" Margin="0,0,0,25"/>

            <TextBlock Text="Tên nhân viên" FontWeight="SemiBold" Foreground="#4A4A4A" Margin="0,0,0,5"/>
            <TextBox x:Name="txtName" Height="35" VerticalContentAlignment="Center" Margin="0,0,0,15" Padding="5,0"/>

            <TextBlock Text="Số điện thoại (Dùng làm tài khoản)" FontWeight="SemiBold" Foreground="#4A4A4A" Margin="0,0,0,5"/>
            <TextBox x:Name="txtPhone" Height="35" VerticalContentAlignment="Center" Margin="0,0,0,15" Padding="5,0"/>

            <TextBlock Text="Mật khẩu" FontWeight="SemiBold" Foreground="#4A4A4A" Margin="0,0,0,5"/>
            <PasswordBox x:Name="txtPassword" Height="35" VerticalContentAlignment="Center" Margin="0,0,0,15" Padding="5,0"/>

            <Grid Margin="0,0,0,25">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Chức vụ" FontWeight="SemiBold" Foreground="#4A4A4A" Margin="0,0,0,5"/>
                    <ComboBox x:Name="cmbRole" Height="35" VerticalContentAlignment="Center">
                        <ComboBoxItem Content="Nhân viên" IsSelected="True"/>
                        <ComboBoxItem Content="Quản lý"/>
                    </ComboBox>
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <TextBlock Text="Lương/Giờ (VNĐ)" FontWeight="SemiBold" Foreground="#4A4A4A" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtSalary" Height="35" VerticalContentAlignment="Center" Padding="5,0" Text="20000"/>
                </StackPanel>
            </Grid>

            <Button Content="LƯU TÀI KHOẢN" Height="45" Background="#10B981" Foreground="White" FontWeight="Bold" Cursor="Hand" Click="btnLuu_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
    </Grid>
</Window>