<Window x:Class="PBL3.GUI.wTrangChu_Boss"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Hệ Thống Quản Lý - BOSS TTN MilkTea" Height="768" Width="1366"
        WindowStartupLocation="CenterScreen" Background="#F0F2F5"
        UseLayoutRounding="True" SnapsToDevicePixels="True" TextOptions.TextFormattingMode="Display">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#111827">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="30,30,0,40">
                    <TextBlock Text="&#xE7EF;" FontFamily="Segoe MDL2 Assets" FontSize="24" Foreground="#F59E0B" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock Text="BOSS" FontSize="20" FontWeight="Heavy" Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>

                <TextBlock Text="ĐIỀU HÀNH TỔNG THỂ" Foreground="#6B7280" FontSize="12" FontWeight="SemiBold" Margin="30,0,0,15"/>

                <StackPanel Margin="15,0">
                    <RadioButton Style="{DynamicResource MenuButtonStyle}" Content="Tổng quan doanh thu" Tag="&#xE80F;" IsChecked="True" Checked="btnTongQuan_Checked"/>
                    <RadioButton Style="{DynamicResource MenuButtonStyle}" Content="Quản lý Nhân sự" Tag="&#xE716;" Checked="btnNhanSu_Checked"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="80"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="White" BorderBrush="#E5E7EB" BorderThickness="0,0,0,1" Padding="30,0">
                <Grid>
                    <TextBlock Text="Xin chào Boss!" FontSize="20" FontWeight="Bold" Foreground="#1A1A1A" VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <TextBlock Text="Đăng xuất" Foreground="#EF4444" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,15,0" Cursor="Hand" MouseLeftButtonDown="lblDangXuat_Click"/>
                        <Border Background="#FEF3C7" Width="40" Height="40" CornerRadius="20">
                            <TextBlock Text="&#xE77B;" FontFamily="Segoe MDL2 Assets" FontSize="18" Foreground="#D97706" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Row="1" Margin="25" Background="White" CornerRadius="12">
                <Border.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="15" ShadowDepth="2" Opacity="0.04"/>
                </Border.Effect>
                <ContentControl x:Name="MainContent" />
            </Border>
        </Grid>
    </Grid>

    <Window.Resources>
        <Style x:Key="MenuButtonStyle" TargetType="RadioButton">
            <Setter Property="Height" Value="50"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#9CA3AF"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10" Padding="15,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{TemplateBinding Tag}" FontFamily="Segoe MDL2 Assets" FontSize="18" VerticalAlignment="Center" Margin="0,0,15,0"/>
                                <TextBlock Text="{TemplateBinding Content}" FontSize="15" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1F2937"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Background" Value="#F59E0B"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
</Window>