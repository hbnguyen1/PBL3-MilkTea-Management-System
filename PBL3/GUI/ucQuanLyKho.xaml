<UserControl x:Class="PBL3.GUI.ucQuanLyKho"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="1100"
             UseLayoutRounding="True" 
             SnapsToDevicePixels="True"
             Background="#F5F6F8">

    <Grid Margin="25">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="6.5*"/>
            <ColumnDefinition Width="3.5*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" CornerRadius="12" BorderBrush="#E5E7EB" BorderThickness="1" Margin="0,0,20,0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.04" Color="#000000"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="20,20,20,15">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Kho Nguyên Liệu" FontSize="22" FontWeight="Heavy" Foreground="#1A1A1A"/>
                        <TextBlock Text="Kiểm soát số lượng và giá nhập nguyên liệu." Foreground="#6B7280" FontSize="13" Margin="0,4,0,0"/>
                    </StackPanel>

                    <Button x:Name="btnRefresh" HorizontalAlignment="Right" VerticalAlignment="Center" Click="btnRefresh_Click" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="#F3F4F6" CornerRadius="8" Padding="15,8">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="&#xE72C;" FontFamily="Segoe MDL2 Assets" Foreground="#4A4A4A" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                        <TextBlock Text="Làm mới" Foreground="#4A4A4A" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>

                <DataGrid x:Name="dgKho" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" 
                          Background="Transparent" BorderThickness="0,1,0,0" BorderBrush="#E5E7EB" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#F3F4F6"
                          RowHeight="50" CanUserAddRows="False">

                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F8F9FA"/>
                            <Setter Property="Foreground" Value="#6B7280"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Padding" Value="15,15"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#E5E7EB"/>
                        </Style>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="15,0"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Border Padding="{TemplateBinding Padding}" Background="Transparent">
                                            <ContentPresenter VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Mã" Binding="{Binding igID}" Width="60" FontWeight="Bold"/>
                        <DataGridTextColumn Header="Tên Nguyên Liệu" Binding="{Binding igName}" Width="*"/>
                        <DataGridTextColumn Header="Số Lượng" Binding="{Binding igCount}" Width="100"/>
                        <DataGridTextColumn Header="Đơn Vị" Binding="{Binding unit}" Width="80" Foreground="#6B7280"/>
                        <DataGridTextColumn Header="Giá Nhập" Binding="{Binding price, StringFormat='{}{0:N0} đ'}" Width="130" Foreground="#4CAF50" FontWeight="SemiBold"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <Border Grid.Column="1" Background="White" CornerRadius="12" BorderBrush="#E5E7EB" BorderThickness="1" Padding="25">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.04" Color="#000000"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="Chi Tiết / Nhập Hàng" FontSize="20" FontWeight="Heavy" Foreground="#1A1A1A" Margin="0,0,0,25"/>

                <TextBlock Text="Tên nguyên liệu" Margin="0,0,0,6" Foreground="#4A4A4A" FontWeight="SemiBold" FontSize="13"/>
                <Border BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="6" Margin="0,0,0,15" Background="#F9FAFB">
                    <TextBox x:Name="txtTen" Height="40" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="12,0" FontSize="14"/>
                </Border>

                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Số lượng" Margin="0,0,0,6" Foreground="#4A4A4A" FontWeight="SemiBold" FontSize="13"/>
                        <Border BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="6" Background="#F9FAFB">
                            <TextBox x:Name="txtSoLuong" Height="40" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="12,0" FontSize="14"/>
                        </Border>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Đơn vị" Margin="0,0,0,6" Foreground="#4A4A4A" FontWeight="SemiBold" FontSize="13"/>
                        <Border BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="6" Background="#F9FAFB">
                            <ComboBox x:Name="cmbDonVi" Height="40" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="8,0" FontSize="14">
                                <ComboBoxItem Content="g" IsSelected="True"/>
                                <ComboBoxItem Content="ml"/>
                                <ComboBoxItem Content="hộp"/>
                            </ComboBox>
                        </Border>
                    </StackPanel>
                </Grid>

                <TextBlock Text="Giá nhập (đ)" Margin="0,0,0,6" Foreground="#4A4A4A" FontWeight="SemiBold" FontSize="13"/>
                <Border BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="6" Margin="0,0,0,35" Background="#F9FAFB">
                    <TextBox x:Name="txtGia" Height="40" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="12,0" FontSize="14"/>
                </Border>

                <Button x:Name="btnThem" Click="btnThem_Click" Cursor="Hand" Margin="0,0,0,12">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="#4CAF50" CornerRadius="8" Height="45">
                                <TextBlock Text="THÊM MỚI" Foreground="White" FontWeight="Heavy" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <Button x:Name="btnCanhBao" Click="btnCanhBao_Click" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="#FFF1F0" BorderBrush="#FFCDD2" BorderThickness="1" CornerRadius="8" Height="45">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock Text="&#xE783;" FontFamily="Segoe MDL2 Assets" Foreground="#E53935" Margin="0,0,8,0" FontSize="16" VerticalAlignment="Center"/>
                                    <TextBlock Text="XEM HÀNG SẮP HẾT" Foreground="#E53935" FontWeight="Heavy" FontSize="14" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>