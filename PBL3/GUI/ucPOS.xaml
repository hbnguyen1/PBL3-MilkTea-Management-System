<UserControl x:Class="PBL3.GUI.ucPOS"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             UseLayoutRounding="True" SnapsToDevicePixels="True" TextOptions.TextFormattingMode="Display"
             Background="#Eef2f6" FontFamily="Segoe UI">

    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5*"/>
            <!-- Cột trái: Menu món -->
            <ColumnDefinition Width="5*"/>
            <!-- Cột phải: Hóa đơn & Thanh toán -->
        </Grid.ColumnDefinitions>

        <!-- ================= CỘT TRÁI: MENU CHỌN MÓN ================= -->
        <Border Grid.Column="0" Background="White" CornerRadius="12" Margin="0,0,15,0" Padding="15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.05"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <!-- Thanh tìm kiếm & Danh mục -->
                    <RowDefinition Height="*"/>
                    <!-- Danh sách món -->
                </Grid.RowDefinitions>

                <!-- Header: Tìm kiếm & Phân loại -->
                <Grid Grid.Row="0" Margin="0,0,0,15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <!-- Dòng 1: Ô tìm kiếm -->
                        <RowDefinition Height="Auto"/>
                        <!-- Dòng 2: Các nút lọc -->
                    </Grid.RowDefinitions>

                    <!-- Ô tìm kiếm (Nằm trọn dòng 1) -->
                    <Border Grid.Row="0" Background="#F3F4F6" CornerRadius="8" Padding="10,5" Margin="0,0,0,10">
                        <!-- Thay StackPanel bằng Grid để TextBox tự động Stretch (co giãn) đầy viền -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="&#xE721;" FontFamily="Segoe MDL2 Assets" Foreground="#9CA3AF" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <!-- Đã bỏ Width="300" để ô tìm kiếm tự động kéo dài ra cho đẹp -->
                            <TextBox Grid.Column="1" x:Name="txtTimKiem" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" FontSize="14" Tag="Tìm kiếm món ăn..." TextChanged="txtTimKiem_TextChanged"/>
                        </Grid>
                    </Border>

                    <!-- Các nút phân loại (Nằm dòng 2, dùng WrapPanel để tự động rớt dòng nếu màn hình nhỏ) -->
                    <WrapPanel Grid.Row="1" Orientation="Horizontal">
                        <!-- Nút BEST SELLER -->
                        <Button Content="🔥 BEST SELLER" Margin="0,0,8,8" Background="#FF6B6B" Foreground="White" Padding="15,8" Cursor="Hand" Click="btnLocMenu_Click" FontWeight="Bold">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="8"/>
                                </Style>
                            </Button.Resources>
                        </Button>

                        <!-- Nút Tất cả -->
                        <Button Content="Tất cả" Margin="0,0,8,8" Background="#1A1A1A" Foreground="White" Padding="15,8" Cursor="Hand" Click="btnLocMenu_Click">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="8"/>
                                </Style>
                            </Button.Resources>
                        </Button>

                        <!-- Nút Trà Sữa -->
                        <Button Content="Trà Sữa" Margin="0,0,8,8" Background="#F3F4F6" Foreground="#4A4A4A" Padding="15,8" Cursor="Hand" Click="btnLocMenu_Click">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="8"/>
                                </Style>
                            </Button.Resources>
                        </Button>

                        <!-- Nút Trà Trái Cây -->
                        <Button Content="Trà Trái Cây" Margin="0,0,8,8" Background="#F3F4F6" Foreground="#4A4A4A" Padding="15,8" Cursor="Hand" Click="btnLocMenu_Click">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="8"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </WrapPanel>
                </Grid>

                <!-- Vùng hiển thị các món ăn (Sẽ đổ dữ liệu bằng ItemsControl sau) -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <!-- Tạm thời dùng WrapPanel để test giao diện, sau này sẽ bind dữ liệu thật -->
                    <WrapPanel x:Name="wpMenu" ItemWidth="180" ItemHeight="120">
                        <!-- Nút Món Ăn Mẫu (Sẽ tạo động bằng C#) -->
                    </WrapPanel>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- ================= CỘT PHẢI: HÓA ĐƠN & THANH TOÁN ================= -->
        <Border Grid.Column="1" Background="White" CornerRadius="12" Padding="15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.05"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <!-- Chọn Khách hàng -->
                    <RowDefinition Height="*"/>
                    <!-- Chi tiết giỏ hàng -->
                    <RowDefinition Height="Auto"/>
                    <!-- Tổng tiền & Nút chốt đơn -->
                </Grid.RowDefinitions>

                <!-- 1. THÔNG TIN KHÁCH HÀNG -->
                <Border Grid.Row="0" Background="#F9FAFB" CornerRadius="8" Padding="12" Margin="0,0,0,15" BorderBrush="#E5E7EB" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Thông tin Khách hàng" FontWeight="Bold" Foreground="#1A1A1A" Margin="0,0,0,10"/>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <RadioButton x:Name="rdoKhachLe" Content="Khách vãng lai" IsChecked="True" Margin="0,0,15,0" Checked="rdoLoaiKhach_Checked"/>
                            <RadioButton x:Name="rdoKhachThanhVien" Content="Thành viên" Checked="rdoLoaiKhach_Checked"/>
                        </StackPanel>

                        <!-- Khung nhập SĐT (Chỉ hiện khi chọn Thành viên) -->
                        <Grid x:Name="gridKhachHang" IsEnabled="False" Opacity="0.5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Border Background="White" CornerRadius="6" BorderThickness="1" BorderBrush="#D1D5DB" Padding="8,5">
                                <TextBox x:Name="txtSdtKhach" BorderThickness="0" Background="Transparent" VerticalContentAlignment="Center" ToolTip="Nhập SĐT khách hàng..."/>
                            </Border>
                            <Button Grid.Column="1" Click="btnTimKhach_Click" Content="Kiểm tra" Background="#2196F3" Foreground="White" Padding="10,5" Margin="5,0,0,0" Cursor="Hand">
                                <Button.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="6"/>
                                    </Style>
                                </Button.Resources>
                            </Button>
                        </Grid>
                        <TextBlock x:Name="txtTenKhachHang" Text="Khách lẻ (Không tích điểm)" Foreground="#6B7280" FontStyle="Italic" Margin="0,8,0,0" FontSize="12"/>
                    </StackPanel>
                </Border>

                <!-- 2. CHI TIẾT GIỎ HÀNG -->
                <DataGrid x:Name="dgGioHang" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" 
                          Background="Transparent" BorderThickness="0" RowHeight="50" Margin="0,0,0,15">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Tên món" Binding="{Binding itemName}" Width="100"/>
                        <DataGridTextColumn Header="Size" Binding="{Binding size}" Width="40" />
                        <DataGridTemplateColumn Header="SL" Width="60">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <Button Content="−" Click="btnGiamSL_Click" Tag="{Binding itemID}" Foreground="#666" Background="#F0F0F0" BorderBrush="#D0D0D0" BorderThickness="1" Width="28" Height="28" Cursor="Hand" FontWeight="Bold" Padding="0" FontSize="14">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="4"/>
                                                </Style>
                                            </Button.Resources>
                                        </Button>
                                        <TextBlock Text="{Binding quantity}" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10,0" TextAlignment="Center" Width="20" FontWeight="Bold"/>
                                        <Button Content="+" Click="btnTangSL_Click" Tag="{Binding itemID}" Foreground="#666" Background="#F0F0F0" BorderBrush="#D0D0D0" BorderThickness="1" Width="28" Height="28" Cursor="Hand" FontWeight="Bold" Padding="0" FontSize="14">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="4"/>
                                                </Style>
                                            </Button.Resources>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Thành tiền" Binding="{Binding total, StringFormat='{}{0:N0} đ'}" Width="80" Foreground="#F28500" FontWeight="Bold"/>
                        <DataGridTemplateColumn Header="Hành động" Width="70">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0">
                                        <Button Content="✎" Click="btnSuaMon_Click" Tag="{Binding itemID}" Foreground="White" Background="#2196F3" BorderThickness="0" Width="25" Height="25" Cursor="Hand" FontWeight="Bold" Padding="0" Margin="0,0,5,0"/>
                                        <Button Content="X" Click="btnXoaMon_Click" Tag="{Binding itemID}" Foreground="White" Background="#F44336" BorderThickness="0" Width="25" Height="25" Cursor="Hand" FontWeight="Bold" Padding="0"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- 3. TỔNG TIỀN & THANH TOÁN -->
                <Border Grid.Row="2" Background="#FFF8E1" CornerRadius="8" Padding="15">
                    <StackPanel>
                        <Grid Margin="0,0,0,10">
                            <TextBlock Text="Tổng cộng:" FontSize="16" Foreground="#4A4A4A" VerticalAlignment="Center"/>
                            <TextBlock x:Name="txtTongTien" Text="0 đ" FontSize="24" FontWeight="Heavy" Foreground="#E65100" HorizontalAlignment="Right"/>
                        </Grid>

                        <Button Click="btnThanhToan_Click" Cursor="Hand" Height="50">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="#4CAF50" CornerRadius="8">
                                        <TextBlock Text="THANH TOÁN" Foreground="White" FontWeight="Heavy" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>